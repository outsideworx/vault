app:
  clients:
    ciafo:
      origin: "*"
      token: "test"
    peeps:
      origin: "*"
      token: "test"
  services:
    grafana:
      url: "http://localhost:3000"
    loki:
      url: "http://localhost:3100"
server:
  port: 80
  ssl:
    enabled: false
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/
    username: postgres
    password: