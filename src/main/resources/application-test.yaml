app:
  clients:
    ciafo:
      origin: "*"
      token: "test"
    peeps:
      origin: "*"
      token: "test"
  services:
    grafana:
      url: "http://localhost:3000"
server:
  port: 8080
  ssl:
    enabled: false
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/
    username: postgres
    password: